{
  "name": "rwer-wasm-tests",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test tests/api.test.mjs",
    "build:wasm": "cd .. && wasm-pack build crates/rwer-wasm --target web --out-dir ../../pkg --out-name rwer && node -e \"const f='pkg/package.json',p=JSON.parse(require('fs').readFileSync(f,'utf8'));p.name='rwer';require('fs').writeFileSync(f,JSON.stringify(p,null,2)+'\\n')\""
  }
}
